=============
Kalau ada masalah di xampp
    FILE php.ini , dihilangkan tanda ; didepannya
    extension=intl
    extension=zip

=============

install yang diperlukan sampai dengan bisa membuka admin panel
    composer install

    APP_KEY=

    DB_CONNECTION=mysql
    DB_HOST=127.0.0.1
    DB_PORT=3306
    DB_DATABASE=f1
    DB_USERNAME=root
    DB_PASSWORD=

    Install Filament
    composer require filament/filament
    php artisan filament:install --panels

    Ketika menghidupkan kembali aplikasinya
    menjalankan composer install lagi
    kemudia refresh server xampp lagi


==========================================

selanjutnya user
https://filamentphp.com/docs/3.x/panels/installation
name : admin
email : admin@email.com
password : 123

=========================

Buat halaman baru
three models and migrations
    php artisan make:model Owner -m
    php artisan make:model Patient -m
    php artisan make:model Treatment -m

Use the following basic schemas for your database migrations:
copy paste di halaman migration

// create_owners_table
Schema::create('owners', function (Blueprint $table) {
    $table->id();
    $table->string('email');
    $table->string('name');
    $table->string('phone');
    $table->timestamps();
});

setelah membuat model dan migration serta mengisi kolom tabelnya

selanjutnya melakukan migrate nanti akan dibuatkan 3 tabel sesuai model yang telah kita buat

Unguarding all models

Let’s set up relationships between the models
antara OWner Patient dan Treatment

php artisan make:filament-resource Patient

muncul halaman patient

========================
Mebuat halaman baru

import function semuanya satu satu di halaman patientresource
halaman owner halaman patient dan halaman treament

kita buat form

kita buat relasi antara owner dengan hewan peliharaannya
        return $form
            ->schema([
                TextInput::make('name')
                    ->required(),

selanjutnya menampilkan di table

========================

Mengisi kolom tabel secara otomatis di patient resourch
        return $table
            ->columns([
                TextColumn::make('name'),

Search able dan filter searchable
            ->filters([
                SelectFilter::make('type')

 TextColumn::make('name')
                    ->searchable(),

===========================

ada kesalahan di branch ketika memunculkan description hewan
jadi langsung ke branch 4 kita mulai ulang

php artisan make:filament-relation-manager PatientResource treatments description

===========================
selanjutnya kita buat halaman owner

php artisan make:filament-resource Owner --generate
ini membuat semua tabel dan form yang dibutuhkan secara otomatis berdasarkan informasi tabel yang kita punya

kayaknya cukup sampai disini kita coba push di branch selanjutnya

===========================

ada masalah sama git branch nya karena tidak bisa ditambah branch yang baru
jadinya kita tambhkan ke branch 4

===========================

selanjutnya kita tambahkan widget
php artisan make:filament-widget TreatmentsChart --chart

dan menambahkan bar grafik
composer require flowframe/laravel-trend
